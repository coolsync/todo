You don’t have to initialize a local variable where it’s declared 您不必在声明的地方初始化局部变量

then the expensive _readThermometer() function is never called: 然后永远不会调用昂贵的 _readThermometer() 函数：


intend to      打算


implicitly     隐式     


isn’t restricted        不限于


type checks and casts      类型检查和强制转换 

